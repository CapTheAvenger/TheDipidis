{
  "name": "thedipidis-bot",
  "version": "0.1.0",
  "description": "Telegram bot for thedipidis.app — Meta Call snapshots + deck builder",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "postinstall": "node -e \"if (!process.env.PUPPETEER_SKIP_DOWNLOAD || process.env.PUPPETEER_SKIP_DOWNLOAD === 'false') { require('child_process').execSync('npx --yes puppeteer browsers install chrome', { stdio: 'inherit' }); }\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "puppeteer": "^23.11.1",
    "telegraf": "^4.16.3"
  }
}
